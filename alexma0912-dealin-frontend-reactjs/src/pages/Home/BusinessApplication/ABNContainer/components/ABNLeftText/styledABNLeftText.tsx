import styled from 'styled-components';

const StyledABNLeftText = styled.div`
  flex-shrink: 0;
  margin-right: 1rem;
`;

const StyledTextSpan = styled.span`
  font-size: 1.2rem;
  font-weight: 500;
  display: inline-block;
  padding-top: 0.5rem;
`;

export { StyledABNLeftText, StyledTextSpan };
